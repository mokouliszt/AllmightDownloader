import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.overlaydl',
  appName: 'AllmightDownloader',
  webDir: 'dist',
};

export default config;
